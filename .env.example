# Copy values into frontend/.env.local for Next.js local development.
NEXT_PUBLIC_MAP_PROVIDER=mapbox
NEXT_PUBLIC_MAP_API_KEY=replace_with_map_api_key
NEXT_PUBLIC_MAP_TILE_URL=https://{s}.tile.openstreetmap.org/{z}/{x}/{y}.png

# Optional backend port
PORT=4000
